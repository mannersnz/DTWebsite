---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, astro.config.mjs, tailwind.config.mjs, src/pages/index.astro, public/digital_tempo_horizontal_small.png]
autonomous: true
---

<objective>
Initialize Astro project with Tailwind CSS and establish project structure for Digital Tempo website.

Purpose: Create the foundation all subsequent phases will build upon - tech stack, folder structure, and assets.
Output: Working Astro project with Tailwind CSS, ready for layout and component development.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro project with Tailwind CSS</name>
  <files>package.json, astro.config.mjs, tailwind.config.mjs, tsconfig.json, src/pages/index.astro</files>
  <action>
    1. Run `npm create astro@latest . -- --template minimal --install --no-git` in project root
       - Use minimal template (clean slate)
       - Install dependencies automatically
       - Skip git init (already have repo)
    2. Run `npx astro add tailwind --yes` to add Tailwind CSS integration
    3. Verify astro.config.mjs includes tailwind() integration
    4. Verify tailwind.config.mjs exists with content paths configured

    Tech stack rationale:
    - Astro: Perfect for static marketing sites, excellent performance, simple mental model
    - Tailwind: Rapid utility-first styling, consistent design system, no separate CSS files
  </action>
  <verify>
    - npm run dev starts server without errors
    - http://localhost:4321 loads default Astro page
  </verify>
  <done>Astro project runs with Tailwind CSS integration active</done>
</task>

<task type="auto">
  <name>Task 2: Create project folder structure</name>
  <files>src/layouts/.gitkeep, src/components/.gitkeep</files>
  <action>
    Create standard Astro folder structure:
    1. src/layouts/ - for page layouts (MainLayout.astro will go here)
    2. src/components/ - for reusable components (Header, Footer, etc.)

    Add .gitkeep files to track empty directories.

    Structure will be:
    ```
    src/
      components/   <- reusable UI components
      layouts/      <- page layout templates
      pages/        <- routes (index.astro already exists)
    public/         <- static assets (logo will go here)
    ```
  </action>
  <verify>Directories exist: src/layouts/, src/components/</verify>
  <done>Folder structure matches standard Astro patterns</done>
</task>

<task type="auto">
  <name>Task 3: Add Digital Tempo logo to assets</name>
  <files>public/digital_tempo_horizontal_small.png</files>
  <action>
    1. Check if logo exists elsewhere in project or needs to be created as placeholder
    2. If logo not available, create a placeholder text noting where to add actual logo:
       - Create public/images/ directory
       - Add README noting: "Place digital_tempo_horizontal_small.png here"
    3. If logo available, copy to public/images/digital_tempo_horizontal_small.png

    For now, assume logo needs to be sourced from existing site. Create placeholder structure:
    - public/images/logo-placeholder.txt with instructions

    Note: Logo should be sourced from existing www.digitaltempo.nz site by project owner.
  </action>
  <verify>public/images/ directory exists with placeholder or actual logo</verify>
  <done>Logo asset location prepared, ready for actual logo file</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts server successfully
- [ ] Tailwind CSS is configured (check for tailwind.config.mjs)
- [ ] Folder structure exists: src/layouts/, src/components/
- [ ] public/images/ directory ready for logo
- [ ] No npm errors or warnings
</verification>

<success_criteria>

- Astro project initialized and running
- Tailwind CSS integrated and configured
- Standard folder structure in place
- Asset directory ready for branding
- Project ready for layout development (Plan 01-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
