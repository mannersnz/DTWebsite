---
// Contact - Bold geometric layout with prominent CTA
---

<section id="contact" class="relative py-24 md:py-32 overflow-hidden">
  <!-- Background -->
  <div class="absolute inset-0 bg-[var(--color-base)]"></div>

  <!-- Grid overlay -->
  <div class="absolute inset-0 grid-overlay opacity-20"></div>

  <!-- Glowing orb -->
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] bg-[var(--color-accent)]/5 rounded-full blur-[150px]"></div>

  <!-- Geometric elements -->
  <div class="absolute top-20 left-10 w-24 h-24 border border-[var(--color-accent)]/20 rotate-45"></div>
  <div class="absolute bottom-20 right-10 w-40 h-40 border border-[var(--color-border)] rotate-12"></div>

  <div class="relative z-10 max-w-5xl mx-auto px-6">
    <!-- Section header -->
    <div class="text-center mb-16">
      <div class="inline-flex items-center gap-4 mb-6">
        <div class="w-12 h-[1px] bg-[var(--color-accent)]"></div>
        <span class="text-[var(--color-accent)] font-display text-sm uppercase tracking-[0.3em]">Get in Touch</span>
        <div class="w-12 h-[1px] bg-[var(--color-accent)]"></div>
      </div>

      <h2 class="font-display font-bold text-4xl md:text-5xl lg:text-6xl text-white mb-6">
        Ready to set the
        <span class="text-gradient"> tempo</span>?
      </h2>

      <p class="text-xl text-[var(--color-text-secondary)] max-w-2xl mx-auto">
        Book a free consultation to discover how AI can transform your business.
        No commitment, just a conversation.
      </p>
    </div>

    <!-- Contact cards -->
    <div class="grid md:grid-cols-2 gap-6 mb-12">
      <!-- Email -->
      <a
        href="mailto:hello@digitaltempo.nz"
        class="group relative bg-[var(--color-surface)] border border-[var(--color-border)] p-8 transition-all duration-500 hover:border-[var(--color-accent)] hover:transform hover:-translate-y-1"
      >
        <!-- Accent line -->
        <div class="absolute top-0 left-0 w-0 h-1 bg-[var(--color-accent)] group-hover:w-full transition-all duration-500"></div>

        <div class="flex items-center gap-6">
          <div class="w-16 h-16 flex items-center justify-center bg-[var(--color-accent)]/10 text-[var(--color-accent)] group-hover:bg-[var(--color-accent)] group-hover:text-[var(--color-base)] transition-all duration-500">
            <svg class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
            </svg>
          </div>
          <div>
            <p class="text-xs uppercase tracking-wider text-[var(--color-text-muted)] mb-1">Email</p>
            <p class="font-display font-semibold text-xl text-white group-hover:text-[var(--color-accent)] transition-colors duration-300">
              hello@digitaltempo.nz
            </p>
          </div>
        </div>

        <!-- Arrow -->
        <div class="absolute bottom-6 right-6 opacity-0 translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all duration-300">
          <svg class="w-6 h-6 text-[var(--color-accent)]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </div>
      </a>

      <!-- Phone -->
      <a
        href="tel:+6421567119"
        class="group relative bg-[var(--color-surface)] border border-[var(--color-border)] p-8 transition-all duration-500 hover:border-[var(--color-accent)] hover:transform hover:-translate-y-1"
      >
        <!-- Accent line -->
        <div class="absolute top-0 left-0 w-0 h-1 bg-[var(--color-accent)] group-hover:w-full transition-all duration-500"></div>

        <div class="flex items-center gap-6">
          <div class="w-16 h-16 flex items-center justify-center bg-[var(--color-accent)]/10 text-[var(--color-accent)] group-hover:bg-[var(--color-accent)] group-hover:text-[var(--color-base)] transition-all duration-500">
            <svg class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
            </svg>
          </div>
          <div>
            <p class="text-xs uppercase tracking-wider text-[var(--color-text-muted)] mb-1">Phone</p>
            <p class="font-display font-semibold text-xl text-white group-hover:text-[var(--color-accent)] transition-colors duration-300">
              +64 21 567 119
            </p>
          </div>
        </div>

        <!-- Arrow -->
        <div class="absolute bottom-6 right-6 opacity-0 translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all duration-300">
          <svg class="w-6 h-6 text-[var(--color-accent)]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </div>
      </a>
    </div>

    <!-- Primary CTA -->
    <div class="text-center">
      <a href="mailto:hello@digitaltempo.nz?subject=AI%20Consultation%20Request" class="btn-primary text-lg py-4 px-10">
        Book Your Free Consultation
        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </a>

      <p class="mt-8 text-sm text-[var(--color-text-muted)]">
        Based in New Zealand  •  Serving businesses nationwide
      </p>
    </div>
  </div>
</section>
